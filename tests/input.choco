let name = input("What's your name? ");
print("Oh! Hello #{name}, nice to meet you.");

print "#{name}, why don't you try my simple calculator?";
let a = input("Enter first number: ");
let b = input("Enter second number: ");

let num1 = float(a);
let num2 = float(b);

let sum = num1 + num2;
print "Sum: #{sum}";

let secret = random_int(1, 100);
let guess = 0;
let attempts = 0;

print "Guess a number between 1 and 100!";

while guess != secret {
    let input_str = input("Your guess: ");
    guess = int(input_str);
    attempts = attempts + 1;
    
    if guess < secret {
        print "Too low!";
    } else {
        if guess > secret {
            print "Too high!";
        }
    }
}

print "Correct! You guessed it in #{attempts} attempts!";